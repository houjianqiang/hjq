git生产分支

目前项目git新建了pro分支，用于生产和发布。
pro分支 和 master或者dev 最大的区别是，包含了生产文件的差异：
1.config-server 的 远程git地址;
2.commodity-server 扫码上传图片静态页面上写死的生产配置。


使用：
1. 切换到生产分支：git checkout  pro
2. 从master或dev分支拉取最新的更新： git pull origin master
3. 解决冲突，若使用默认的冲突描述文件，看到nano界面 点击ctrl+x 即可退出
4. 同步更新到pro的远程库，git push origion pro
5. 执行发布任务
6. 还原到开发分支： git checkout master
